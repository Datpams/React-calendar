//All code in Web/React-calendar/scripts/components everything else is just to render and all pre required components
//Importing App component which is everything
import React from 'react';
import App from './App';
//Rendering the final application
React.render(<App />, document.body);
